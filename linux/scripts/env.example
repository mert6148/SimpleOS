# Gerçek Linux çekirdeği için dışarıdan derlenen modül örneği (ileride kullanım):
# LINUX_SRC=/usr/src/linux-headers-$(uname -r)
# ARCH=x86_64 CROSS_COMPILE= make -C $(LINUX_SRC) M=$(PWD) modules
#
# Bu maket şu anda `kernel/kernel.c`'yi kullanıcı alanı simülasyonu olarak derlemektedir.
